name: provider
version: 1.0.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.16.8

dependency_overrides:
  test_api: ^0.3.0
