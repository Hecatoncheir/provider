name: provider
version: 1.0.1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints: ^1.0.1
  flutter_lints: ^1.0.4
  dart_code_metrics: ^4.1.0

  test: ^1.17.10

dependency_overrides:
  test_api: ^0.4.0
